# Open Questions

1. Implement and test CustomFieldDefinition, CustomFieldOption, and CustomFieldValue APIs with centralized type/reference validation.
2. Add ProceedingDocument linking and unlinking endpoints.
3. Add Document/Activity API-client methods and replace Phase 03 empty states with real Angular integrations.
4. Add the metadata-driven custom-field renderer and Matter/Client form integration.
5. Add authenticated API/browser coverage and remote tenant verification.
