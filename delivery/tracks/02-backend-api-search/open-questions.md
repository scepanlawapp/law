# Open Questions

1. Add participant-role and proceeding mutation endpoints before Phase 02 can be considered feature-complete against the full prompt.
2. Add authenticated API e2e coverage for cross-tenant IDs, search leakage, lifecycle, and lookup deactivation.
3. Resolve and test legal lookup seeding for newly provisioned tenants without restoring the reverted TenantRegistryService behavior.
4. Reconcile stale frontend API clients and legacy Case vocabulary during Phase 03.
