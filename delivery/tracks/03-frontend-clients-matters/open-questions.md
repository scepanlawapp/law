# Open Questions

1. Add the backend participant/role and Proceeding mutation endpoints required by the detail management UI.
2. Complete repeatable contact point, identifier, address, organization-contact, Matter-client, participant, and proceeding form controls.
3. Implement inline lookup creation with refresh/select behavior.
4. Add authenticated browser verification after `web-e2e` receives a configured target.
5. Replace hardcoded residual legacy wording and add complete SR/EN keys for the new Matter terminology.
