# Plan

- [x] Register this track in `delivery/index.md`.
- [x] Put `briefResultId` on the completed brief-extraction job so the UI can load the suggestion without a reload.
- [x] Render the matter-link card inside the conversation, only after a brief exists.
- [x] Load the suggestion when extraction finishes and when an existing session is opened.
- [x] Keep the chat column at full width when no draft review pane is open.
