# evaluation

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build evaluation` to build the library.

## Running unit tests

Run `nx test evaluation` to execute the unit tests via [Jest](https://jestjs.io).
