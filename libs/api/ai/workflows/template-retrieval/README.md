# template-retrieval

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build template-retrieval` to build the library.

## Running unit tests

Run `nx test template-retrieval` to execute the unit tests via [Jest](https://jestjs.io).
