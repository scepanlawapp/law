# brief-extraction

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build brief-extraction` to build the library.

## Running unit tests

Run `nx test brief-extraction` to execute the unit tests via [Jest](https://jestjs.io).
