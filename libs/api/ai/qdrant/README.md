# qdrant

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build qdrant` to build the library.

## Running unit tests

Run `nx test qdrant` to execute the unit tests via [Jest](https://jestjs.io).
