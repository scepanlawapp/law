# n8n

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build n8n` to build the library.

## Running unit tests

Run `nx test n8n` to execute the unit tests via [Jest](https://jestjs.io).
